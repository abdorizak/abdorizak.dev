// Content (text, cards) max width in px
export const MAX_SITE_WIDTH = 620;

// Dark background max width in px.
// Make this > MAX_SITE_WIDTH to widen the dark area
// without widening the content.
export const MAX_BG_WIDTH = 520;
